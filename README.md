# Lab Website

Built with Hugo Blox. Edit `data/` YAML files for content, `config/_default/` for site settings.

Example data files live in `docs/examples/` (copy one into the matching `data/` folder, rename it to `<slug>.yaml`, and uncomment). Do not leave non-YAML files inside `data/`; Hugo tries to parse everything there.
